<?xml version="1.0" encoding="utf-8" ?>
<config>
    <tenant></tenant>
    <app_id></app_id>

    <services>
        <service name="resources">1</service>
        <service name="exchange">1</service>
    </services>

    <outputFiles>0</outputFiles>    <!-- Enable output cleartext files -->
    <outputMLA>1</outputMLA>    <!-- Enable output encrypted MLA archive -->

    <requestsThreads>100</requestsThreads>
    <writerThreads>10</writerThreads>
</config>